{
  "name": "subconvert-worker",
  "version": "0.2.0",
  "description": "A Cloudflare Worker to convert between different subscription formats.",
  "main": "_worker.js",
  "scripts": {
    "deploy": "npm run build && wrangler deploy",
    "build": "npm ci & wrangler deploy --dry-run",
    "dev": "wrangler dev --env dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230710.1",
    "wrangler": "^4.20.5"
  },
  "dependencies": {
    "js-yaml": "^4.1.0",
    "zod": "^4.0.5"
  }
}
